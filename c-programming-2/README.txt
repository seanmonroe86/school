README

Compile the source code file 'derive.c' using either gcc or cc.
Execute 'derive' with './'

Example:

gcc derive.c -o derive
./derive

or

cc derive.c -o derive
./derive

Note:
All instances of 'derive' in this README may be replaced with another
filename, such as 'derive.out'.
