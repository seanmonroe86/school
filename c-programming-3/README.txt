Compile the source code files 'dealer.c' and 'cards.c' with gcc.

Execute 'dealer' with './' with valid arguments A B, where

1 <= A <= 13
1 <= B <= 13
A * B <= 52

and are integers.

Example:

gcc dealer.c cards.c -o dealer
./dealer 5 5
